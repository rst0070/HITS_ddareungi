
export type StopInfo = {
    stop_id: string,
    rank : number,
    score: String,
    address: string,
    latitude: number,
    longitude: number
  }

export type CenterInfo = {
    id: string,
    latitude: number,
    longitude: number
  }